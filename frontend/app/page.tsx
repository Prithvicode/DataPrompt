import ChatComponent from "@/components/Chat";

import React from "react";

const page = () => {
  return (
    <div className="bg-accent">
      <ChatComponent />
    </div>
  );
};

export default page;
